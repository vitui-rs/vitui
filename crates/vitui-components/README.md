# vitui-components

Windows, panels, charts, lists, trees, forms and pickers built on
[`vitui-runtime`](../vitui-runtime).

**Empty. There is no code here yet** — one `lib.rs` with module documentation and no components.

The architecture is settled and sliced into 43 tickets; none has been implemented. The crate exists
so the workspace shape and the dependency direction are fixed before anything is written into it.

Do not depend on this. It is published as part of the workspace and it does nothing.

## Status

Nothing to report but the absence, which is why this file says so rather than describing components
that do not exist. Follow [`vitui-runtime`](../vitui-runtime) — this crate starts when that one
finishes.

## Licence

MIT OR Apache-2.0, at your option.
